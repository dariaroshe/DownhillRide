using System;
using Game;
using UnityEngine;

namespace Menu
{
    public class MenuStarter : MonoBehaviour
    {
        public MenuScene MenuScene;

        private void Start()
        {
            var menuModel = new MenuModel();
        }
    }
}